<para>Globus Standard</para>

<table><title>TITLE</title>
<tgroup cols="4"><tbody>
<row>
 <entry><emphasis>Name</emphasis></entry>
 <entry><emphasis>Value</emphasis></entry>
 <entry><emphasis>Description</emphasis></entry>
 <entry><emphasis>Comments</emphasis></entry>
</row>

<row>
 <entry>GLOBUS_LOCATION</entry>
 <entry>&lt;path&gt;</entry>
 <entry>The &lt;path&gt; is the root location of the Java WS Core installation. 
     Must be an absolute path.
 </entry>
 <entry>Required</entry>
</row>

<row>
 <entry>GLOBUS_TCP_PORT_RANGE</entry>
 <entry>&lt;min,max&gt;</entry>
 <entry>The &lt;min,max&gt; is the minimum and maximum port range for TCP server sockets (usefull for systems behind firewalls). For example, if set, the notification sink on the client will be started within that port range.
 </entry>
 <entry>Optional</entry>
</row>

<row>
 <entry>GLOBUS_HOSTNAME</entry>
 <entry>&lt;host&gt;</entry>
 <entry>The &lt;host&gt; is either a hostname or ip address. The host ip address under which the container and services will be exposed.
 </entry>
 <entry>Optional</entry>
</row>

</tbody></tgroup>
</table>

<para>Launch script specific</para>

<table><title>TITLE</title>
<tgroup cols="4"><tbody>
<row>
 <entry><emphasis>Name</emphasis></entry>
 <entry><emphasis>Value</emphasis></entry>
 <entry><emphasis>Description</emphasis></entry>
 <entry><emphasis>Comments</emphasis></entry>
</row>

<row>
 <entry>GLOBUS_OPTIONS</entry>
 <entry>&lt;arguments&gt;</entry>
 <entry>
   The &lt;arguments&gt; are arbitrary arguments that can be passed to the JVM.
   See below for detailed list of supported options.</entry>
 <entry>Optional</entry>
</row>

<row>
 <entry>JAVA_HOME</entry>
 <entry>&lt;path&gt;</entry>
 <entry>The &lt;path&gt; is the root location of the JVM installation. If set, JVM from that installation will be used. Otherwise, the first one found in path will be used.
 </entry>
 <entry>Optional</entry>
</row>

<row>
 <entry>CLASSPATH</entry>
 <entry>&lt;classpath&gt;</entry>
 <entry>This environment property is ignored by launch scripts.
 </entry>
 <entry>Ignored</entry>
</row>
</tbody></tgroup>
</table>


<para>
Options supported by the <computeroutput>GLOBUS_OPTIONS</computeroutput> environment property:
</para>

   <table><title>TITLE</title>
<tgroup cols="4"><tbody>
   <row>
     <entry><emphasis>Name</emphasis></entry>
     <entry><emphasis>Value</emphasis></entry> 
     <entry><emphasis>Description</emphasis></entry>
     <entry><emphasis>Comments</emphasis></entry>
   </row>
   
   <row>
     <entry>-Dorg.globus.wsrf.proxy.port</entry>
     <entry>int</entry>
     <entry>This property specifies the port number of the proxy server. The proxy server must run on the same machine as the container. This setting will cause the service address to have the port of the proxy instead of the container (only applies to computeroutput that uses <computeroutput>ServiceHost</computeroutput> or <computeroutput>AddressingUtils</computeroutput> API.</entry>
     <entry>Standalone container only</entry>
   </row>

   <row>
     <entry>-Dorg.globus.wsrf.container.server.id</entry>
     <entry>string</entry>
     <entry>This property specifies the server id, a name that is used to distinguish between multiple containers running on the same host for persistence purposes. By default the container will store the persistent resources under <computeroutput>~/.globus/persisted/&lt;hostname&gt;/</computeroutput>. If there are multiple containers running on the same machine the same directory will be used for the persistent resources which might lead to some collisions. This property controls which storage directory is used by the container. If set, the container will store the persisted resources under <computeroutput>~/.globus/persisted/&lt;server.id&gt;/</computeroutput> instead.</entry>
    <entry>Standalone container only</entry>
   </row>

</tbody></tgroup>
</table>





