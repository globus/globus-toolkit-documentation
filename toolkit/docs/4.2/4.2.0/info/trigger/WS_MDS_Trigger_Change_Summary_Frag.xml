<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.4//EN"
"http://www.oasis-open.org/docbook/xml/4.4/docbookx.dtd">

<section id="trigger-changessummary">
  <title>Summary of Changes in WS MDS Trigger Service</title>
<indexterm type="trigger"><primary>changes summary</primary></indexterm>
  <para>The following changes have occurred for MDS4 Trigger Service since the
  last stable release, <replaceable
  role="entity">previousversion</replaceable>:</para>

  <para>New Features:</para>

  <itemizedlist>
    <listitem>
      <simpara>Multiple triggers per data source aggregation is now supported.
      Each trigger's Matching Rule will be evaluated against the incoming
      message.</simpara>
    </listitem>

    <listitem>
      <simpara>Support for XML Namespace resolution in the Matching Rule Xpath
      statement. This allows namespace prefixes to be specified in the
      TriggerRule XPath matching rule string parameter.</simpara>
    </listitem>
  </itemizedlist>

  <para>Changes:</para>

  <itemizedlist>
    <listitem>
      <simpara>Triggers are now created/configured in a completely different
      manner from previous versions. They are created dynamically, as
      needed.</simpara>
    </listitem>

    <listitem>
      <simpara>Some parameter names have been changed, added, or
      removed.</simpara>
    </listitem>
  </itemizedlist>
</section>