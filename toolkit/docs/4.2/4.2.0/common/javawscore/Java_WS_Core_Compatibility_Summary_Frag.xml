<?xml version='1.0' encoding='UTF-8'?>
<para>Protocol changes since GT version &previousversion;</para>
<itemizedlist>
  <listitem>
    <simpara>HTTP/1.1 with &apos;chunked&apos; transfer encoding is now used by default.</simpara>
  </listitem>
  <listitem>
    <simpara>Wire messages follow the new schemas and therefore are completely different (see below).</simpara>
  </listitem>
</itemizedlist>
<para>API changes since GT version &previousversion;</para>
<itemizedlist>
  <listitem>
    <simpara>The majority of the APIs are new. Some APIs resemble GT 3.2 APIs, for example <computeroutput>ServiceData</computeroutput> is replaced by <computeroutput>ResourceProperty</computeroutput> and <computeroutput>ServiceDataSet</computeroutput> is replaced by <computeroutput>ResourcePropertySet</computeroutput>.</simpara>
  </listitem>
</itemizedlist>
<para>Schema changes since GT version &previousversion;</para>
<itemizedlist>
  <listitem>
    <simpara>Schemas are completely new. The WS Java Core implements the OASIS WSRF and WSN working drafts specifications (with minor fixes to the 1.2-draft-01 published schemas and with the March 2004 version of the WS-Addressing specification.)</simpara>
  </listitem>
</itemizedlist>
