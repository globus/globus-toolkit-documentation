<?xml version='1.0' encoding='UTF-8'?>

<para>
<emphasis>The job manager detected an invalid script response</emphasis>
</para>
<itemizedlist>
    <listitem><simpara>Check for a restrictive umask. When the service writes the native
        scheduler <glossterm linkend="job-description">job description</glossterm> to a file, an overly restrictive umask will
        cause the permissions on the file to be such that the submission
        script run through <glossterm linkend="sudo">sudo</glossterm> as the user cannot read the file (bug #2655).
    </simpara></listitem>
</itemizedlist>

<para>
<emphasis>Fork jobs work fine, but submitting PBS jobs with globusrun-ws hangs at "Current job state: Unsubmitted"</emphasis>
</para>
<itemizedlist>
    <listitem><simpara>Make sure the the log_path in
    $GLOBUS_LOCATION/etc/globus-pbs.conf points to locally accessible scheduler
    logs.  The Scheduler Event Generator (SEG) will not work without local scheduler
    logs to monitor.  This can also apply to other resource managers, but is
    most comonly seen with PBS.
    </simpara></listitem>
    <listitem><simpara>If the SEG configuration is sane, then you will have to
    debug the PBS adapter to figure out what error is not being propagated back
    to the client.  See <xref linkend="wsgram-developer-debugging"/> in the Developer's Guide.
    </simpara></listitem>
</itemizedlist>

<para>
<emphasis>When restarting the container, I get the following error:
Error getting delegation resource</emphasis>
</para>
<itemizedlist>
    <listitem><simpara>Most likely this is simply a case of the delegated
    credential expiring.  Either refresh it for the affected job or destroy
    the job resource.
    </simpara></listitem>
</itemizedlist>
