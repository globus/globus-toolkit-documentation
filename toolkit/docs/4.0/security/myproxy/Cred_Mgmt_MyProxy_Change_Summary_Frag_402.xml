<?xml version='1.0' encoding='UTF-8'?>

<para>The following changes have occurred for MyProxy:</para>
<itemizedlist>
<listitem>Updated GPT package from 2.3 to 3.5.</listitem>
<listitem>API changes: new trusted_retrievers member for
    struct myproxy_creds and struct myproxy_request_t</listitem>
<listitem>Added ability for myproxy-server to act as a CA by setting
    certificate_issuer options in myproxy-server.config.</listitem>
<listitem>Added support for CA username to DN resolution via LDAP using
    myproxy-server.config ca_ldap options.</listitem>
<listitem>Dropped support for certificate_issuer option in
    myproxy-server.config; use certificate_issuer_cert instead.</listitem>
<listeitem>Added support for certificate-only authentication to the
    myproxy-server via myproxy-init -Z and myproxy-server.config
    trusted_retrievers options.</listitem>
<listitem>Added myproxy-server authentication support for Pubcookie
    (http://www.pubcookie.org/) granting cookie via
    pubcookie_granting_cert and pubcookie_app_server_key
    myproxy-server.config options.</listitem>
<listitem>Added myproxy-init --local_proxy option to create a local proxy
    credential after storing a credential on the myproxy-server.</listitem>
<listitem>Added myproxy-init --keyfile/--certfile options.</listitem>
<listitem>Install example myproxy-server.config to
    $GLOBUS_LOCATION/share/myproxy instead of potentially overwriting
    existing version in $GLOBUS_LOCATION/etc.</listitem>
<listitem>In myproxy-logon, added support for writing credential to standard
    output via '-o -' option; also added --quiet option.</listitem>
<listitem>Added myproxy-logon --no_credentials option to authenticate
    without retrieving credentials.</listitem>
<listitem>Added certificate_mapapp call-out for mapping usernames to
    certificate subject distinguished names for the CA module.</listitem>
<listitem>added certificate_extfile and certificate_extapp (call-out) for
    setting extensions in certificates issued by CA module.</listitem>
</itemizedlist>
