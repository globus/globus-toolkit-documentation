<?xml version='1.0' encoding='UTF-8'?>

<para>The following changes have occurred for MyProxy since GT 4.0.2:</para>
<itemizedlist>
<listitem><simpara>Updated GPT package from 3.5 to 3.6.</simpara></listitem>
<listitem><simpara>Added support for credential renewal via the MyProxy CA using
    myproxy-server.config authorized_renewers/default_renewers options</simpara></listitem>
<listitem><simpara>Provided an access control list for storing credentials via the
    myproxy-server.config accepted_credentials_mapfile option</simpara></listitem>
<listitem><simpara>Added support for VOMS attributes in myproxy-server.config policies</simpara></listitem>
<listitem><simpara>Changed MyProxy CA toissue certificates valid starting 5 minutes in the past to
    account for possible clock skew between hosts</simpara></listitem>
<listitem><simpara>Added check_multiple_credentials option in myproxy-server.config
    to allow clients to retrieve a credential for a given username
    even if the associated "credential name" isn't provided (via
    myproxy-logon --credname)</simpara></listitem>
</itemizedlist>
