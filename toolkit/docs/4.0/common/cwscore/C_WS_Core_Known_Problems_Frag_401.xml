<?xml version='1.0' encoding='UTF-8'?>
<para>The following problems are known to exist for C WS Core at the time of the 4.0.1 release:</para>
<itemizedlist>
  <listitem><simpara>SOAP faults without header elements are not parsed correctly.</simpara></listitem>
  <listitem><simpara>Multiple schemas which use the same namespace prefixes can confuse the WSDL parser.</simpara></listitem>
  <listitem><simpara>Nillable elements are not serialized or deserialized correctly if
  the element does not contain the minOccurs="0" attribute</simpara></listitem>
  <listitem><simpara>Errors that occur when processing resource properties on
  systems which crash when the printf format %s is matched with a null parameter
  will crash the c service container.</simpara></listitem>
</itemizedlist>
