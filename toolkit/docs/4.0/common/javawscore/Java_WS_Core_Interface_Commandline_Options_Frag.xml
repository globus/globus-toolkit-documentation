<refsect1><title>Common Command Line Options</title>
<para>
Some clients share the same command line options:
</para>

<table><title>TITLE</title>
<tgroup cols="2"><tbody>

<row>
 <entry><command>-h, --help</command></entry>
 <entry><para>Displays help information about the command.</para></entry>
</row>

<row>
 <entry><command>-d, --debug</command></entry>
 <entry><para>Enables debug mode. For example, full stack traces of errors will be displayed.</para></entry>
</row>

<row>
 <entry><command>-e, --eprFile &lt;file&gt;</command></entry>
 <entry><para>Specifies an XML file that contains the endpoint reference</para></entry>
</row>

<row>
 <entry><command>-s, --service &lt;url&gt;</command></entry>
 <entry><para>Specifies the service URL.</para></entry>
</row>

<row>
 <entry><command>-k, --key &lt;name value&gt;</command></entry>
 <entry><para>Specifies the resource key. The <command>name</command> is the QName of the
     resource key in the string form: <command>{namespaceURI}localPart</command>, while
     the <command>value</command> is the simple value of the key. For complex keys,
     use the <command>--eprFile</command> option. Example: 
   <screen>-k "{http://www.globus.org}MyKey" 123</screen></para></entry>
</row>

<row>
 <entry><command>-f, --descriptor &lt;file&gt;</command></entry>
 <entry><para>Specifies a client security descriptor. Overrides all other security settings.</para></entry>
</row>

<row>
 <entry><command>-a, --anonymous</command></entry>
 <entry><para>Enables anonymous authentication. Only supported with transport security or GSI Secure Conversation authentication mechanism.</para></entry>
</row>

<row>
 <entry><command>-g, --delegation &lt;mode&gt;</command></entry>
 <entry><para>Enables delegation. <command>mode</command> can be either <command>'limited'</command> or <command>'full'</command>.  Only supported with GSI Secure Conversation authentication mechanism.</para></entry>
</row>

<row>
 <entry><command>-l, --contextLifetime &lt;value&gt;</command></entry>
 <entry><para>Sets the lifetime of the client security context. <command>value</command> is in milliseconds.  Only supported with GSI Secure Conversation authentication mechanism.</para></entry>
</row>

<row>
 <entry><command>-m, --securityMech &lt;type&gt;</command></entry>
 <entry><para>Specifies the authentication mechanism. <command>type</command> can be <command>'msg'</command> for GSI Secure Message, or <command>'conv'</command> for GSI Secure Conversation.</para></entry>
</row>

<row>
 <entry><command>-c, --serverCertificate &lt;file&gt;</command></entry>
 <entry><para>Specifies the server's certificate file used for encryption. Only needed for GSI Secure Message authentication mechanism.</para></entry>
</row>

<row>
 <entry><command>-p, --protection &lt;type&gt;</command></entry>
 <entry><para>Specifies the protection level. <command>type</command> can be <command>'sig'</command> for signature or <command>'enc'</command> for encryption.</para></entry>
</row>

<row>
 <entry><command>-z, --authorization &lt;type&gt;</command></entry>
 <entry><para>Specifies authorization type. <command>type</command> can be <command>'self'</command>, <command>'host'</command>, or <command>'none'</command>.</para></entry>
</row>
</tbody></tgroup>
</table>

</refsect1></refentry>
