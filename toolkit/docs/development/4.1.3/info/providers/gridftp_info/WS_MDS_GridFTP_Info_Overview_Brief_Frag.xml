<?xml version='1.0' encoding='UTF-8'?>

<!-- This is where you describe the information provider (pulled into both the release notes and the reference page). 
This should be simply one paragraph, so paste your text in between the para tags below. -->

<para>

This provider connects to a gridftp server, reads its banner, and puts
the banner in the index.  If the connection cannot be made in 30
seconds, or some other error occurs the server is marked as down.

This package contains execution aggregator providers.  The providers
are quite useful as is and additionally serve as an example of how
to create others.  The perl scripts and configuration files can
be modified to the users liking once installed.

Each provider has a perl script and an xml configuration file.  The
perl script is used to obtain information and formated the information
into xml output.  The output is written to stdout where it is gathered

</para>