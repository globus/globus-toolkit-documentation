<para>The Trigger Service itself does not have any command-line clients for end
users; instead,
the trigger service is configured to run an executable program to take some
action (for example, send mail to a set of users, or write a log entry to
    a file).</para>
    <para>The <ulink url="http://www.globus.org/toolkit/docs/4.0/info/aggregator/rn01re02.html"><command>mds-servicegroup-add</command></ulink> command in the Aggregator Framework is used to configure the
    Trigger Service to collect information from various sources. </para>

