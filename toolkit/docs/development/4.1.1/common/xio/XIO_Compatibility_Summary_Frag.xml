<para>Protocol changes since GT version 3.2</para>
<itemizedlist>
  <listitem><simpara>None.</simpara></listitem>
</itemizedlist>
<para>API changes since GT version 3.2</para>
<itemizedlist>
    <listitem><simpara>
        globus_xio_stack_copy added to the API.  This allows a user to 
        duplicated a configured stack.
    </simpara></listitem>
    <listitem><simpara>
        globus_xio_driver_set_eof_received added to the driver API.
        This function allows drivers to have multiple outstanding
        reads at one time.
    </simpara></listitem>
    <listitem><simpara>
        globus_xio_driver_eof_received added to the driver API.
        Working in conjunction with globus_xio_driver_set_eof_received
        to allow drivers to have multiple outstanding reads.
    </simpara></listitem>
    <listitem><simpara>
        Users can now passing in a NULL callback for timeouts and it is
        assumed that when time expires the user wants the operation to
        timeout.  Previously a user callback was required where the user
        would decide if they wanted the timeout.
    </simpara></listitem>
</itemizedlist>
